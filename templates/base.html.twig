<!DOCTYPE html>
<html>
	<head>
		<title>Hackathon - Application WEB</title>
		<meta charset="UTF-8">
		<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
		<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
		<style>
			button {}
		</style>
		<body>
			<div class="container">
				<nav class="navbar navbar-expand-lg">
					<a class="navbar-brand" href="{{path('index')}}">HackatWeb</a>
				</button>

				<div class="collapse navbar-collapse" id="navbarSupportedContent">
					<ul class="navbar-nav mr-auto">
						<li class="nav-item active">
							<a class="nav-link" href="{{path('index')}}">Accueil</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{path('ListeHack')}}">Liste des Hackathons</a>
						</li>
					</ul>
					{% if is_granted('IS_AUTHENTICATED_FULLY') %}
						<div class="authentification_button">
							<a type="button" href="{{ path('profil')}}" class="btn btn-primary">Profil</a>
							<a type="button" href="{{ path('security_logout')}}" class="btn btn-primary">Déconnexion</a>
						</div>
					{% else %}
						<ul class="nav navbar-nav navbar-right">
							<div class="authentification_button">
								<a type="button" href="{{ path('security_login')}}" class="btn btn-primary">Connexion</a>
								<a type="button" href="{{ path('app_register')}}" class="btn btn-primary">Inscription</a>
							</div>
						</ul>
					{% endif %}
				</div>
			</div>
		</body>
	</head>
</html>
